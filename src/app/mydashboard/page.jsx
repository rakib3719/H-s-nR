import React from 'react';
import Mydashboard from '../[component]/Mydashboard/Mydashboard';

const page = () => {
    return (
        <div>
            <Mydashboard/>
        </div>
    );
};

export default page;